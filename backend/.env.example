# SQLite connection string (default shown).
DATABASE_URL=sqlite:///./fireflies.db

# Comma-separated list of allowed CORS origins (the frontend URL).
CORS_ORIGINS=http://localhost:3000

# Set to 0 to disable automatic seeding of sample meetings on startup.
AUTO_SEED=1
